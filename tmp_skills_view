
        <!-- <h3 class="skills__subtitle subtitle">...Front-End JavaScript Technologies</h3> -->

        <!--<div layout="column" class="skills__container">

            <div ng-repeat="(skill_key,skills_group) in skills.front" layout-sm="column" layout-gt-sm="row" layout-align="center center" class="skills__content {{skill_key}}" >

                <div ng-repeat="skill in skills_group" flex-sm="60" flex-gt-sm="{{ 100 / skills_group.length }}" class="md-icon__container" title="{{skill.name}}">

                    <svg ng-if="skill.svg" class="md-icon" role="img" title="{{skill.name}}">
                        <use xlink:href="{{ '#' + skill.icon }}"/>
                    </svg>
                    <img ng-if="!skill.svg" ng-src="{{'images/tools_icons/' + skill.icon +'.png'}}" class="md-icon" alt="{{skill.name}}" />

                </div>

            </div>

        </div> -->

<!--         <h3 class="skills__subtitle subtitle">...Back-End JavaScript Technologies & DevTools</h3> -->

<!--         <div layout="column" class="skills__container">

            <div ng-repeat="(skill_key,skills_group) in skills.back" layout-sm="column" layout-gt-sm="row" layout-align="center center" class="skills__content" >

                <div ng-repeat="skill in skills_group" flex-sm="60" flex-gt-sm="{{ 100 / skills_group.length }}" class="md-icon__container" title="{{ skill.name }}">

                    <md-icon-svg ng-if="skill.svg" icon-sprite="{{ '#' + skill.icon }}" class="md-icon__container" title="{{ skill.name }}"></md-icon-svg>
                    <img ng-if="!skill.svg" ng-src="{{ 'images/tools_icons/' + skill.icon +'.png'}}" class="md-icon" alt="{{ skill.name }}" />

                </div>
            </div>
        </div>
        -->
<!--
        <h3 class="skills__subtitle subtitle">...other Technologies occasionally</h3>

        <div layout="column" class="skills__container">

            <div ng-repeat="(skill_key,skills_group) in skills.others" layout-sm="column" layout-gt-sm="row" layout-align="center center" class="skills__content" >

                <div ng-repeat="skill in skills_group" flex-sm="60" flex-gt-sm="{{ 100 / skills_group.length }}" class="md-icon__container" title="{{ skill.name }}">

                    <md-icon-svg ng-if="skill.svg" icon-sprite="{{ '#' + skill.icon }}" class="md-icon__container" title="{{ skill.name }}"></md-icon-svg>
                    <img ng-if="!skill.svg" ng-src="{{ 'images/tools_icons/' + skill.icon +'.png'}}" class="md-icon" alt="{{ skill.name }}" />

                </div>
            </div>

        </div> -->
